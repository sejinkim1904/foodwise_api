{
  "name": "foodwise_api",
  "version": "1.0.0",
  "description": "",
  "main": "source/app.ts",
  "scripts": {
    "build": "rm -rf build/ && prettier --write source/ && tsc",
    "test": "jest --config ./jest.config.ts --forceExit --coverage --runInBand",
    "start": "nodemon source/app.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/body-parser": "^1.19.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "travis-ci": "^2.2.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "jest": "^27.0.6"
  }
}
